/** @format */

import { StyleSheet } from "react-native";
import { CHIP_HEIGHT } from "../../Utils/Constants/Constants";

const styles = StyleSheet.create({
  containerStyle: {
    width: "100%",
    height: CHIP_HEIGHT + 6,
  },
});

export default styles;
